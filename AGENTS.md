# 项目说明
目前项目还在开发阶段，一些重构和修改不用考虑历史数据的兼容，可以直接清理

## 约束

1. 后端文件代码行数不要超过2000行
2. 修改前检查 Git 工作树。已有修改默认属于用户，不得覆盖、回退或删除无关内容。
3. 如果实现需要改变已确定的架构边界，先说明原因和影响，并同步更新相关设计文档。
4. 整体功能需要有 E2E 端到端自动化测试，单元测试
5. 选能满足当前需求的最简单实现。不要预防性抽象，不要多此一举的配置层。 
6. 先翻项目里已有的依赖能做什么，再考虑加新包或自己写。别上来就假设库里没有。 
7. 架构决策往长了做。不接受"先这样以后再换"的临时方案。 
8. 先看成熟产品怎么解决同一个问题，用已验证的模式，别从零发明。